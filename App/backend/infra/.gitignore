# CDK synthesized output
cdk.out/

# CDK-managed context cache (auto-written lookups only). This project's deploy
# context — authCallbackUrls, authLogoutUrls, cognitoDomainPrefix — is committed
# in cdk.json's `context` block, not here.
cdk.context.json

node_modules/
