# CDK synthesized output
cdk.out/

# CDK-managed context cache. Also holds this project's manually-set deploy
# context (authCallbackUrls, authLogoutUrls, cognitoDomainPrefix) — kept local
# per developer/environment. If you'd rather commit stable values, move them to
# the `context` block in cdk.json instead.
cdk.context.json

node_modules/
